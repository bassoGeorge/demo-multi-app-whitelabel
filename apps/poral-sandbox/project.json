{
  "name": "portal-sandbox",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/poral-sandbox",
  "projectType": "application",
  "tags": ["scope:portal", "client:sandbox", "type:app"],
  "// targets": "to see all targets run: nx show project portal-sandbox --web",
  "targets": {
    "assets": {
      "executor": "@multi-app-whitelabel/nx-plugin:assets",
      "options": {
        "from": ["p-shared-assets"]
      }
    }
  },
  "implicitDependencies": ["p-shared-assets"]
}
