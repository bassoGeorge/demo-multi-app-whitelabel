import { designSystemUtils } from './design-system-utils';

describe('designSystemUtils', () => {
  it('should work', () => {
    expect(designSystemUtils()).toEqual('design-system-utils');
  });
});
